{
  apps : [
    {
      name      : "GrahpQ Animation",
      script    : "app.coffee",
      env : {
        NODE_ENV: "production"
      }
    }
  ],
  deploy : {
    production : {
      key  : "pem/graphq.pem",
      user : "ubuntu",
      host : "52.11.211.11",
      ref  : "origin/master",
      repo : "https://github.com/graphiq-os/animation.git",
      path : "/srv/production",
      "pre-deploy": "",
      "post-deploy" : "source ~/.profile && sudo npm install && sudo bower install --allow-root && sudo pm2 startOrRestart ecosystem.json5 --env production"
    }
  }
}
